"""
    The whole logic of this module is invoked by the stats() method. The whole idea is to
    take a time period as arguments and calculate statistics with them. 

    #### Contant configuration variables:
    - **MAX_PAGE_COUNT** (int): Prevents the application from making many requests in case the given time period is too large.
    - **CUSTOM_FILTER** (str): The most important filter of the Stackexchange's API since it enables the use to customize the given results.
    - **DEFAULT_URL** (str): The Stackexchange's default API url.
"""
pass